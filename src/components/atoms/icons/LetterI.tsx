import {FC} from 'react';
import Icon, {IconProps} from './_Icon';

const LetterI: FC<IconProps> = props => (
  <Icon {...props} source={require('../../../../assets/icons/letter_i.svg')} />
);

export default LetterI;
