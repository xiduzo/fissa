import {FC} from 'react';
import Icon, {IconProps} from './_Icon';

const LetterS: FC<IconProps> = props => (
  <Icon {...props} source={require('../../../../assets/icons/letter_s.svg')} />
);

export default LetterS;
