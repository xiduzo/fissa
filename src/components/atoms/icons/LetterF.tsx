import {FC} from 'react';
import Icon, {IconProps} from './_Icon';

const LetterF: FC<IconProps> = props => (
  <Icon {...props} source={require('../../../../assets/icons/letter_f.svg')} />
);

export default LetterF;
